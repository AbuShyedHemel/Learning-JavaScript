function myMath(){
    let round = Math.round(3.2426)
    let floor = Math.floor(3.1416)
    let ceil = Math.ceil(3.1416)
    let trunc = Math.trunc(3.1416)
    let power = Math.pow(2,2)
    let square = Math.sqrt(4)
    let absolae = Math.abs(-10.5)//absolute (positive) value
    let sine = Math.sin(90 * Math.PI/180)
    let cos = Math.cos(0 * Math.PI/180)
    let random = Math.random()
    document.getElementById("demo").innerHTML= random
}